/* The "const" keyword is used to declare a
   constant variable. Introduced in ES6
   (ECMAScript 2015), const provides a way to
   declare variables that cannot be reassigned
   after their initial declaration. */
var message = "Hello World!";
console.log(message.toLowerCase());
// message();
